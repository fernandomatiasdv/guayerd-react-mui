import { createTheme } from "@material-ui/core/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#ec407a",
      light: "#ff77a9",
      dark: "#b4004e",
    },
    secondary: {
      main: "#8e24aa",
      light: "#c158dc",
      dark: "#5c007a",
    },
  },
});

export default theme;
